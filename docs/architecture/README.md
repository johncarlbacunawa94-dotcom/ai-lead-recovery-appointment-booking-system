# Architecture Documents

This directory contains both locked architecture baselines and
versioned implementation-state snapshots.

## Locked baseline

The primary locked architecture is:

`PHASE1_ARCHITECTURE_LOCK_v1.0.txt`

The approved Phase 1 addendum is:

`PHASE1_ARCHITECTURE_ADDENDUM_v1.1.txt`

These files must not be casually overwritten.

## Current implementation snapshot

Current:

`PROJECT_ARCHITECTURE_CURRENT_v1.4.txt`

Historical snapshots are retained for traceability.

## Phase 6 test-mode authorization

Current:

`PHASE6_TEST_MODE_ADDENDUM_v1.5.txt`

This addendum records the approved distinction between implemented
Twilio provider-contract behavior and deferred live carrier validation.

## Versioning rule

Architecture revisions use new versioned files.

Do not overwrite an older architecture snapshot merely to make it
match the current implementation.

Historical versions remain part of the project audit trail.